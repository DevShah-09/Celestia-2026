# Include /api. Leave unset locally to use Vite's proxy to localhost:3000.
VITE_BACKEND_URL=http://localhost:3000/api

# Production build: set this to your deployed backend HTTPS URL, including /api.
# VITE_BACKEND_URL=https://your-backend.example.com/api
